#include<iostream>
using namespace std;
// amount of time taken by an algorithm to run as a fuction of length of input 
// time taken by code to run on cpu/ time wasted by code of cpu

// O(1)
// O(n)
//big O -> algorithm's upper bound
// theta -> average case
// omega( sign of ohm) -> lower bound

// type of big O
// O(1)- constant time , eg- int a=5
// O(n)- linear time   , eg- for(int i=0;i<N,i++){ cout<<i<<endl;}
// O(N2) - quadratic time; eg- for(int i=0;i<N,i++){for(int j=0;j<N,j++){ cout<<j<<endl;}};
// O(n3) - cubic time,   eg- for(int i=0;i<N,i++){for(int j=0;j<N,j++){ for(int k=0;k<N,k++){ cout<<j<<endl;}}};
// O(log n)- logarithmic time-  binary search

// time complexity in increasing order-> O(1), O(log N), O(root N), O(N), O(Nlog N), O(Nsqare) ie O(N2), O(N3), O(2 power N), O(N!), O(N power N)

int main()
{
    int t;
    cin>>t;

    while(t--){
        int a,b,c;
        cin>>a>>b>>c;
        for(int x=0 ; x>a; x++)
        {
            
        }
        
    }
}
