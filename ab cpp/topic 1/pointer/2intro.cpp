//pointer is a variable used to storing address of data
/*wo type 1)data variable          (int x=5;)
          2) address variable       (int *p;)
          declaration- int *p;
          Initialisation- p=&x;        (&x means location of memory where x is stored And &x is read as address of x)
          dereferencing - cout<<*p;    (derefering means going to that location and taking data ie. 5)
         cout<<*p; means display data where p is pointing
*/         