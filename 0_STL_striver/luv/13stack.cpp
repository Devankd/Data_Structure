#include<bits/stdc++.h>
using namespace std;

// in stack push(t) and pop() are used where in vector push_back() are used

int main(){
    stack<int>s;
    queue<string>q;
    s.push(5);
    s.push(6);
    s.push(7);

    q.push("ttrer");

    cout<<s.empty(); // yes or no
}